# 网页互动壁纸接入方案

## 现状与原因

海面慢慢、云上散步、星点之间是 gallery/preview.html 加载 Three.js/Vanta 脚本生成的 WebGL 场景，并不是 MP4。现有插件 Player.cs 用 WPF MediaElement 播放视频、系统接口设置 PNG；协议只接受视频 ID 和固定静态图 ID，没有 HTML、JavaScript 或 WebGL 运行环境。网页收藏记录不包含可供当前播放器播放的媒体，因此仅修改按钮无法解决。

## 本轮处置

将三项从公开目录、首页和下载列表隐藏，拒绝继续加入下载；原素材与许可保留。可用静态图由 CCBCM 整理展示，详情保留 Daylin Morgan 原作者和 MIT 信息。移除开源作者的独立创作者卡片。既有账号不自动取得上传权，需 CCBCM 审批。

## 路线一：低负担视频版（优先）

固定画面、分辨率、时长和随机种子，离线渲染/录制成无缝循环 H.264 MP4。确认头尾衔接、色彩、体积及授权后走现有上传审核流程。继续使用现有缓存、哈希校验、遮挡暂停。代价是鼠标互动消失，标注为视频动态壁纸，不能冒称互动版。

## 路线二：真正互动版

新增独立 WebView2 渲染进程并嵌入桌面层，按需启动；壁纸采用包含入口、资源清单、版本、哈希和性能预算的受控离线包。首期仅接收平台审核的固定模板，禁止任意远程导航、宿主对象、原生消息桥和系统命令；明确下载、弹窗、权限申请均拒绝。保持原视频进程可用，不把整个插件换为常驻浏览器。

资源来自本地固定映射目录；校验路径、文件数、解压后体积、哈希，防止目录穿越和压缩炸弹。渲染异常独立重启且不能影响插件控制面板。遮挡/游戏暂停时停止 requestAnimationFrame，必要时挂起渲染进程，恢复后重建；帧率默认30、限制像素比与显存负担。

验收覆盖离线启动、切换、桌面点击穿透、不同DPI与多显示器、窗口遮挡、锁屏、休眠恢复、GPU占用、内存持续增长、崩溃恢复、恶意包及权限限制。以实測结果决定是否开放，不承诺零占用。此方案尚未实装，当前插件仍仅支持静态图与视频。
