# CCBCM 创作者平台完善流程

创建日期：2026-09-11

## 续做约定

- 状态：**生效中**。
- 用户在本项目说“继续按流程走”“按流程继续”等同义表达，即要求执行本文件中下一个未完成步骤；先核对项目实际状态，然后实施、验证、保存进度并同步到 ccbcm.net。
- 用户已批准下面的总体方案。常规实现、测试、Git 提交和网站更新不重复询问；涉及必要的新凭据、服务费用或无法自行解决的外部条件时，说明具体所需操作，继续完成不依赖该条件的工作。
- 每次续做阅读本文件，优先修复当前阶段的失败项，不跳过未完成项宣称完成。用户后续明确指令优先。
- 这是续做约定，不是定时后台任务；没有用户触发时不自动启动工作。
- 全部阶段通过验收后，将状态改为“已完成，续做约定失效”，记录最终验收证据，并明确告知用户：**“本项目方案流程已全部完成，‘继续按流程走’这句话正式失效。”** 此后同样措辞不再自动启动新范围的工作。

## 目标与当前基线

目标：以动态壁纸为主的多创作者社区，支持其他创作展示，并通过轻量桌面插件下载、离线切换和应用壁纸。保持圆润、简洁、自然中文 UI；重内容只在用户主动打开后加载。

当前已有 GitHub 登录、D1 账号下载记录、作品浏览和预览、桌面插件及自动部署。已实测 ccbcm 登录、保存“蓝色呼吸”、退出重登后记录保留。此基线不代表上传、多人创作和通用插件下载已完成。

待解决：账号入口与下载入口混淆；作者/作品仍为固定目录；没有作者上传发布后台；收藏仍在浏览器；账号保存与设备下载容易混淆；插件目前只识别内置演示的应用链接。用户报告下载内容被当成自己的作品展示，需复现定位，不能未经验证就认定作者字段被修改。

## 不可混淆的产品规则

| 空间 | 内容 | 可见性 |
| --- | --- | --- |
| 我的下载 | 用户保存的壁纸及设备下载状态 | 默认仅本人 |
| 我的收藏 | 用户感兴趣的作品 | 默认仅本人 |
| 创作中心 | 本人草稿、待审核、已发布、已下架作品 | 本人管理 |
| 创作者主页 | 本人主动公开发布的作品与简介 | 公开 |

- 下载/收藏不得改变作者，不得增加下载者的公开作品数量。
- “全部”只涵盖当前空间，例如“全部下载”。
- 开源收录保留原作者和许可，不能冒充作者入驻。
- 云端保存记录不等于文件已下载到电脑；网站不能在未收到插件确认时声称应用成功。
- GitHub 仅负责身份登录，普通用户无需创建 OAuth 应用或配置密钥。

## 阶段 1：归属、导航和私人空间

- [ ] 复现用户报告的下载/作品混淆，修复路由、筛选和归属问题。
- [x] 顶部：发现、壁纸、创作者、发布作品、头像菜单。
- [x] 个人菜单：我的下载、我的收藏、创作中心、个人主页、账号设置、退出（已支持 GitHub 头像或配色字母头像）。
- [ ] 分离四类空间；明确空列表、未登录、加载失败和重试状态。
- [ ] 昵称、头像、简介与公开主页；收藏改为账号同步，旧本地收藏迁移不得误归属其他账号。
- [x] 将全站大插件横幅收进独立桌面插件页面，在需要时提示安装。
- [x] 陆家嘴保留在 CCBCM 互动作品中，点击后才加载。

验收：下载他人壁纸只进入本人下载；作者和本人公开作品数量不变；私人列表不向其他账号公开；手机/电脑导航清晰，账号切换不串数据。

## 阶段 2：真实创作中心、上传和发布

- [ ] 用持久化作品、作者、文件版本和发布状态替代固定目录；迁移现有内容并保留来源。
- [ ] 流程：上传文件 → 填写信息 → 预览并发布。
- [ ] 支持标题、简介、分类、标签、封面、原创/授权来源；自动识别格式、大小、分辨率和时长。
- [ ] 草稿保存、上传进度、失败重试；草稿、待审核、已发布、已下架管理。
- [ ] 已发布作品可更新同一作品版本，保留链接和已有下载关系；下架与删除区分，避免误删。
- [ ] 用户文件用对象存储，Git 只存代码及适当项目资产；明确容量与上传限额，先确认实际服务条件和费用再启用收费资源。
- [ ] 首期开放图片和视频；网页/可执行/复杂互动内容受控发布，完善隔离和检查前不开放任意执行。
- [ ] 管理后台：审核、修改原因、举报处理、下架、上传限额。
- [ ] 服务端校验文件实际类型、体积和访问权限；不能只信扩展名或前端作者 ID。

验收：两个独立账号可各自上传发布，只能编辑自己的内容；草稿不公开；上传失败可恢复；不合规文件被拒绝；作品更新保持原链接；未获得第二账号真实验收时明确记录未完成，不能用单账号测试替代。

## 阶段 3：网站与桌面插件完整衔接

- [ ] “下载”保存到账号后提供下载并应用；登录中断后可恢复原作品操作。
- [ ] 明确状态：已加入我的下载、正在下载到电脑、已下载、使用中、失败。
- [ ] 通用作品清单和安全下载协议，支持作者新发布的视频，不局限 blue 演示。
- [ ] 本地列表、进度、失败重试、移除本地文件、避免重复下载。
- [ ] 已下载作品离线切换；不支持的格式不给出无效应用按钮。
- [ ] 插件状态确认与网站反馈；适当验证来源、文件完整性和下载参数，避免任意执行。
- [ ] 保留快速启动、可选自启动、遮挡/全屏游戏/电池暂停，实测性能和恢复行为。

验收：新发布视频从网站进入账号库，再下载到电脑、应用成功，断网仍可再次切换；下载/应用失败不显示成功；不得未经实测宣称轻量或零占用。

## 阶段 4：发现、视觉和全流程验收

- [ ] 精选、最新、分类；按类型、风格、横竖屏、分辨率、插件兼容性搜索筛选。
- [ ] 统一圆润卡片、按钮、图标、间距、弹窗关闭位置和自然中文文案。
- [ ] 检查手机与电脑布局、溢出、长标题、空数据、异常反馈、键盘访问。
- [ ] 验证封面优先、主动预览、关闭后释放播放资源；大文件和 3D 不拖累首页。
- [ ] 完整回归浏览、登录、收藏、下载、上传、审核、更新、下架、插件应用。
- [ ] 备份和恢复安排、发布说明、必要的来源/许可与用户数据说明。
- [ ] 更新所有进度、记录证据、确认网站部署版本；宣布续做约定失效。

## 暂缓范围

评论、私信、付费交易及任意可执行内容开放上传暂缓；不纳入本流程的完成条件。新需求应由用户明确增加范围。

## 结构性维护与后续扩展规则

当前项目不是“把所有代码堆在一个页面里”的临时脚本，已经有明确的边界：

- `functions/api/[[path]].js` 负责鉴权、账号、作品、审核和存储接口；网页交互在 `gallery/app.js`，样式在 `gallery/site.css`，构建产物由 `authoring/scripts/build-site.mjs` 生成。
- D1 保存账号、作品状态、收藏/下载和审核关系；R2 保存用户文件；GitHub 只保存代码和适合进入仓库的项目资产。密钥只放 Cloudflare 环境变量，不写入 Git。
- `wrangler.jsonc` 保存公开变量、D1/R2 绑定；机密变量（GitHub secret、Resend key）必须留在 Cloudflare Pages 密钥栏。新增服务先加接口适配层和环境变量，再接 UI，不能在页面里直接写第三方密钥。
- 作品状态、作者归属和用户下载是不同关系；后续功能必须复用现有状态机和会话归属，不能通过浏览器本地存储或固定目录“模拟”云端数据。

客观结论：现有结构可以继续更新，账号、作品、审核、对象存储和插件协议已经有可复用边界；但单体 API 文件仍偏大，媒体类型、版本更新、插件回执和邮箱服务继续增加时，若直接追加分支会增加回归风险。因此后续每个阶段都加入一次结构整理：先抽取纯校验/媒体/权限辅助模块，保持接口路径兼容，再做功能；为每个新状态写接口测试和失败状态，不以单次浏览器成功代替验收；部署前固定提交、构建和线上 HTTP/接口检查。

以后“按流程走”的顺序固定为：

1. 读取本文件并核对 Git、部署、数据库、R2、环境变量和线上版本，先修复当前阶段阻塞。
2. 设计数据关系和接口边界，确认归属、权限、失败回滚和费用，不先堆 UI。
3. 实现服务端校验与持久化，再接网站和插件；保持桌面/手机响应式布局与统一圆角组件。
4. 做针对性自动检查、构建和线上回归；涉及真实账号、上传、外部协议或密钥时单独记录证据。
5. 提交并推送 GitHub，等待 Cloudflare 部署，检查公开页面和 API；把完成项、阻塞项、下一步写回本文件。

当前结构治理状态：已建立模块边界和环境变量规则；尚未完成 API 拆分、媒体版本表、插件状态回执和邮箱真实发信验收。它们属于后续阶段，不把“代码能构建”当成完成。

## 参考

- itch.io 创作者流程：https://itch.io/docs/creators/getting-started
- Wallpaper Engine 发布与更新：https://docs.wallpaperengine.io/scene/first/publishing
- Lively 播放器：https://github.com/rocksdanister/lively

参考其产品流程；使用代码或素材前核对具体许可，不将闭源产品当作可直接复制的源码。

## 进度与接续记录

- 2026-09-11：用户批准将方案保存为项目流程并建立续做约定。本次仅保存方案，未开始上述阶段实现。
- 当前阶段：阶段 1，部分完成，续做约定仍生效。
- 下次起点：补充旧收藏导入的浏览器验收及真实双账号交叉验证；随后推进阶段 2 的持久化作品与真实创作中心。资料、云端收藏已上线，不能再以占位页作为完成成果。
- 每次更新本节应写明：完成项、验证证据、部署提交、未完成项/阻塞、下一步。完成清单须有证据才打勾。

### 阶段 1 第一次推进
- 代码提交：9af3b96，已推送 main；浏览器已确认 ccbcm.net 提供 spaces-1 新版。
- 修复未知 hash 地址回退到全部作品的问题；新增独立个人菜单、创作中心/个人主页/设置入口。后三者目前为明确提示尚未开放的页面，不算完成资料编辑或发布功能。
- 下载卡片标注原作者、私人账号记录与本机保存的区别；公开作者页明确为公开作品；全站插件横幅迁移到 #plugin。
- 线上真实账号验证：ccbcm 保存“海面慢慢”后，#downloads 显示作者 Teng Bao；#creator/ccbcm 仍只有“蓝色呼吸”和“陆家嘴 · 城市漫游”，未将下载变为自己的作品。原有“蓝色呼吸”记录保留。测试新增“海面慢慢”下载记录保留，未删除用户数据。
- 本地浏览器验证：未知路由显示返回发现，发现页正常，插件页独立，个人菜单展开。修复旧 header nav 高度样式造成的菜单挤压；390px 视口文档宽度为 390px、菜单边界在视口内，检查后恢复尺寸。
- 检查通过：node --check gallery/app.js；npm.cmd run build；node authoring/scripts/test-accounts.mjs（未登录、CSRF、无效作品、账号隔离、重复下载、退出、过期、OAuth state、未配置服务）。
- 尚未完成：完整头像导航、云端收藏与迁移、资料编辑及真实公开主页、两个真实账号交叉验证。不能把静态作者目录的验证当作未来数据库发布流程验收。
- 用户最初报告的完整操作路径未获得；本次确认并修复路由兜底混淆，未发现下载会修改作者数据。阶段 1 首项保持未完全勾选，后续在真实创作流程接入后回归。

### 阶段 1 第二次推进
- 代码 ff43118：新增 profiles/favorites 持久化表与接口，支持昵称、300 字简介、GitHub/三种字母头像、公开个人资料、私有账号收藏及取消。表按需幂等创建；未替换原用户、会话、下载表。附 authoring/scripts/spaces-schema.sql。
- 代码 4fa88ab：CCBCM 账号主页保留原有两件原创作品；开源收录和账号下载不混入。作品目录仍待阶段 2 数据库化。另修正收藏初次加载时点击的状态竞争。
- 本地配置提示已确认是静态预览没有 API 而返回非 JSON，被旧文案误写成配置中；现在显示本地预览说明和 https://ccbcm.net/#settings 入口。线上无需再次配置 OAuth。
- 线上验证 ff43118：现有账号读取资料成功，保持原昵称/简介/头像提交，得到“资料已保存”；公开主页可打开。收藏“海面慢慢”后新页面重新读取仍保留；没有写入浏览器收藏来冒充云端同步。测试收藏保留。
- 旧 ccbcm-saved 不自动归属账号：页面显示目标账号，用户明确点击后逐件导入；全部成功才移除旧记录，失败可重试。本次浏览器没有旧记录，完整导入交互尚待验收，不勾选整个资料/收藏迁移组合项。
- 测试通过：资料长度/头像值校验、会话决定归属而非 user_id、匿名拒绝、收藏跨账号隔离/幂等/删除/CSRF、公开资料仅含公开字段、原下载不受影响；已有账号测试和构建通过。
- 手机 390px 检查：设置表单和所有输入框在视口内，无横向溢出；已恢复视口。未把单账号浏览器测试称作双真实账号验收。
- 未完成：真实双账号、旧收藏导入浏览器验收、上传与发布。当前阶段仍为阶段 1 部分完成，续做约定生效。

### 用户调整与阶段 2 草稿基础
- e074653：简介禁用拖拽，输入后自动增高；删除头像选项框，账号设置/本人公开主页点击头像选择 JPG、PNG、WebP。客户端裁为 256px JPEG，独立头像接口限体积、校验 JPEG 尺寸与格式、按会话归属保存。不影响其他资料。非本人主页不显示文件入口。
- 浏览器验证简介框由 79px 随六行文字增至 156px，resize 为 none，测试文字未保存。设置和本人主页均显示选图入口。头像 JPEG 正常持久化、隔离、CSRF、无效格式测试通过；尚未用用户本机照片完成浏览器文件选择全链路，不能声称已实测。
- 8e073a5：推进阶段 2 的私有文字草稿，可保存/继续编辑标题与简介，最多 50 份。后端验证匿名拒绝、跨账号不可修改、更新不新增重复草稿。未开放正式发布或文件上传。
- e348a05：浏览器发现表单 name=id 遮蔽 form.id，修复提交匹配。验收时输入的测试内容不涉及真实用户数据。保存功能须以修复后浏览器结果为准。
- 大文件对象存储在当前 wrangler 配置中尚无绑定。后续需核对 Cloudflare R2 实际服务条件，再接文件上传、持久化作品和发布状态。没有启用收费资源。
- 续做流程仍有效，阶段 1 剩余真实双账号/旧收藏导入验证保留，阶段 2 仅文字草稿基础，不算上传发布完成。
- 最终线上验收（spaces-6 / a5d8a98）：草稿保存成功，列表出现一份“流程验证草稿（未发布）”，继续编辑正确回填标题和简介。该测试草稿保留在本人私有空间。账号设置已打开。

### 头像简化与上传服务核对
- d085807：移除头像“更换”文字及黑色遮罩，保留文件选择及无障碍标签；线上 spaces-7 已验证，设置头像仅显示选图入口。
- Cloudflare 控制台实际核对：R2 尚未开通，进入 /r2/plans。页面说明免费额度 10GB/月，超额存储 $0.015/GB/月，A/B 操作超过免费额度另计费；开通建立订阅并使用已有付款方式。
- 按本流程收费资源确认约定，尚未点击开通。已询问用户是否授权，等待答复；不是 OAuth 配置问题。
- 下次起点：若用户同意 R2 开通，开通后创建项目存储桶并绑定 Pages，继续实施有归属/类型/大小校验的文件上传；若不同意，继续不依赖对象存储的流程项。不得将等待视作授权，不宣称上传已完成。

### R2 开通与网站绑定
- 用户已明确同意带免费额度、超额自动计费的 R2，随后用户自行完成开通。控制台已验证 R2 概述可用。
- 已创建 ccbcm-creator-assets，标准存储、自动亚太位置、公开访问禁用；初始为空桶。
- ec30b56：wrangler.jsonc 添加 CREATOR_ASSETS R2 绑定，通过 Git 部署。新增仅登录可用的只读连接检查 /api/storage，使用 head 不写入测试文件。
- fd267e3：创作中心显示文件存储连接状态；相关未登录/未绑定/可读绑定测试通过，原账号回归与构建通过。
- 后续起点：实现文件上传到私有桶、每个文件与作者/草稿关联、限额预留和失败回收、格式与大小校验，再做真实文件上传验证和发布审核。尚未实现这些，不能因为 R2 开通就宣称创作者可上传发布。
- 规划采用单文件/单账号/全站上传容量限制，但当前未实施，不保证免费额度不被超出。未启用其他付费产品。继续约定有效。
- 线上验收完成：spaces-8 创作中心明确显示“文件存储已连接”，只读 R2 head 经真实部署成功；原验证草稿仍存在。

### 动态视频壁纸上传（9e975b8）
- 创作中心以选择 MP4 为主入口；旧文字草稿折叠保留，不再作为主上传流程。
- 新增 creator_videos 数据及 R2 私有文件关联：预留容量、上传、失败重试、云端预览（Range）、标题简介修改、提交待审核、移除文件释放容量。
- 当前限制：单文件 25 MiB，账号视频/封面预留 250 MiB，全站视频/封面预留 8 GiB；数据库单条条件 INSERT 原子预留，日计数触发器独立保留，删除作品不会重置每日 20 次新上传限制。这不是 R2 账户账单硬限额。
- 客户端读取视频验证可播放、生成 JPEG 封面与文件 SHA256；服务端读取实际字节限制体积、校验完整性及 MP4 box 结构。不宣称完成完整视频转码或恶意内容审核。
- 文件只有本人可读取，未发布不会出现在发现页；提交审核状态已接入，但审核后台和真正上架待后续完成，不能称为公开发布成功。
- 自动测试用现有 blue.mp4 验证正常上传、断传/重试、内容 hash、Range、匿名/跨账号拒绝、删除释放对象、日限额持续、伪 MP4 拒绝；账号回归和构建通过。后续补线上真实文件选择与播放结果。

### 2026-09-12 视频实测与账号审核入口
- 真实浏览器已上传原有 blue.mp4，1280×720、12 秒；视频能够播放，刷新后标题和简介保留，提交后显示待审核。测试作品“蓝色呼吸 · 上传验证”保留在本人账号。
- 按用户新要求添加 #moderation 和个人菜单“作品审核”，服务端 ADMIN_GITHUB_ID 固定为已核对公开头像对应的 325610429；普通账号无权限。可查看所有账号提交的作品、预览、通过或填写原因退回。
- 审核结果与状态使用 D1 batch 事务同步写入；重复审核返回状态冲突。退回原因在作者创作中心展示，作者可修改重提。通过状态暂为 approved，明确标注等待上架功能接入，不冒充已公开。
- 上传删除使用互斥状态锁，简介自动增高；自动测试覆盖匿名/普通账号拒绝、管理员跨作者预览、退回原因、重新提交、通过、重复决定、CSRF 和通过后禁止修改。账号回归、语法、构建通过。
- 下次起点：完成通过审核视频的公开目录、作者主页与下载接入；随后继续其他流程项。审核入口待本次部署浏览器验收；流程约定仍生效。

- 线上 6fa671b 审核页已读取测试视频；实际点击“通过审核”成功，作品变为 approved。b8c7e89 补菜单隐藏规则与卡片间距；review-2 刷新样式缓存。未公开测试视频。

### 卡片排版与公开上架
- 审核卡片正文使用统一 video-copy 容器，桌面 24px、小屏 20px 内边距；新公开作品、下载卡片复用，长文字换行。
- 审核通过后作者可公开上架，进入公开壁纸目录与作者主页；登录用户可加入个人下载/收藏。公开视频按需播放，支持保存到电脑。新视频暂不提供插件一键应用，避免调用尚未支持的协议。
- 作者可下架，公开接口随即拒绝读取；修改后需重新审核，不能直接绕过审核上架。自动测试覆盖公开前不可读、跨作者操作拒绝、上架/下载/收藏、下架禁读和重新审核；回归与构建通过。
- 下一步：浏览器验收发布与排版，然后继续管理员下架/举报、插件新视频对接等未完成项；流程约定仍生效。

- 8bdd1ad 已线上验收：审核标题距卡片左边 24px；页面无横向溢出。实际上架自有“蓝色呼吸 · 上传验证”，壁纸列表和本人公开主页均出现；点击下载进入个人下载，显示原作者和保存到电脑入口。浏览器无错误日志。测试作品及下载记录保留。

### 账号箭头与管理员下架
- 菜单字符箭头替换为 16×10 圆角线条图标，上移 2px，展开时翻转。
- 管理员可对已发布视频填写原因并下架；事务同步审核说明与状态。作者看到原因，重新公开需再次审核。测试覆盖普通账号拒绝、无原因拒绝、下架后公开不可读、不可直接重新上架。构建和账号回归通过。
- 下一步继续举报入口与处理、插件对接；流程仍有效。

### 账号对齐与举报基础
- 头像、账号名、箭头统一 flex 居中和固定间距；箭头下尖按文字底部调整，待浏览器视觉验收。
- 已发布上传视频可举报，需登录并填写原因；同账号同作品防重复，每天最多 10 次。管理员审核页进入用户举报，查看原因、前往审核并标记已处理。只针对平台上传视频，开源静态收录举报尚未接入。
- 接口测试覆盖匿名、重复、无原因、普通账号越权及管理员处理；下一步插件对接和完整流程余项，续做约定仍有效。

### 优先修复网站到插件应用
- 我的下载移除手动文件下载入口，视频改用“使用”协议。新版插件支持已发布视频 ID，固定官网接口、大小上限、SHA256 校验、临时文件写入后原子替换、后台缓存后应用。缓存不放到用户下载目录。
- 旧插件仅支持 blue，已重编译安装包；网页特效和静态收录暂标不支持，未冒充全部格式可用。下架作品 manifest 拒绝读取。下一步线上应用验收；流程仍有效。

- d4f5e67：补充 cat/lines/caffeine/tux 静态 SVG 的 PNG 派生，保留原素材许可；插件通过系统接口应用静态背景。安装包已更新本机。
- 真实验证：我的下载小猫卡片只有“使用”；Chrome 外部协议点击时控制连接中断，未证明浏览器提示已确认。直接调用已安装插件的同一协议成功，系统 Wallpaper 指向 Cache/cat.png。公开视频协议成功 desktop_attached、media_open_ms=284.3，遮挡自动暂停；验证后切回小猫。不能将该日志说成浏览器全链路已确认。

### 使用入口反馈
- 点击“使用”后，卡片提示允许浏览器打开插件，并提供安装/更新入口；不把发出协议请求当作应用成功。
- 当前仅有内置预览浏览器可控制，系统外部协议弹窗全链路仍待真实浏览器验收；之前静态/视频原生应用日志证据保留。
- 忽略 node_modules，避免把构建依赖提交到仓库。构建通过，继续约定有效。

### 下载空间整理
- 正式站连接恢复 200；浏览器实际看到“使用”后的打开插件提示。内置浏览器外部协议最终调用仍不能确认。
- 添加移出我的下载，包含已下架的失效记录；按会话归属删除个人记录，作者文件与发布状态不受影响。跨账号、匿名、CSRF 与正常移除测试覆盖；不删除用户现有记录来测试。
- 下一步继续完整流程验收与插件剩余兼容项，续做约定有效。

### 下载卡片排版修正
- 9ddc10c：上传作品、收录作品与失效记录统一卡片结构、封面比例及标题/作者/类型行高；长文字省略，详情通过作品入口查看。操作行固定间距，应用提示另起一行。
- 正式站浏览器验证六张卡片：第一排按钮 y=743.36，第二排 y=1200.33；按钮高度42，使用与移除间距14，无重叠。构建和语法检查通过。此次修正完成，整体流程仍有效。

### 连接恢复与全站卡片统一（2026-09-12）
- 用户确认错误为 ERR_CONNECTION_CLOSED。本机 Clash 服务日志中 ccbcm.net 经代理节点出现 IPv6 bind6 错误与 IPv4 连接超时；官网及公开接口探测 200，直接出站延迟探测成功。仅为当前订阅添加 DOMAIN-SUFFIX,ccbcm.net,DIRECT 前置规则，配置原文件已就地备份；内核重载 204，后续日志确认 DIRECT，刷新与新标签首次打开成功。此证据定位并处理了本机线路问题，不保证所有网络永不发生连接错误。
- 网站读取请求每次最多10秒，网络/502/503/504 仅自动重试一次；写入不自动重放，401不重试。首屏有可重试占位，公开作品读取失败可在页内重试。故障模拟测试、账号回归和构建通过。
- 发现、壁纸、收藏、作者主页统一 catalog-card；上传与收录不再两套高度。线上九张壁纸卡片文字区均104px，同排收藏按钮纵坐标一致，无文字横向溢出。创作者卡片简介限制两行，按钮靠底，管理卡片操作区明确间距并可换行。
- 已完成本轮异常恢复和阶段4卡片布局推进；全流程仍有双账号真实验收及插件反馈等未完项，约定继续生效。

### 陆家嘴改为私有作品
- 按最新要求移除首页小世界区块和公共目录中的城市作品，发现/收藏/公开作者主页不再展示。仅固定所有者账号的创作中心提供入口，保留工程。
- /city 与 /city.html 服务端校验会话所有者，其他账号及匿名返回404，私有响应禁止缓存。路由授权测试与账号回归、构建通过。此要求替代原先公共互动作品展示要求。

### 插件兼容目录与创作者审批
- 暂时撤下 waves/clouds/net 网页互动壁纸，首页改用蓝色呼吸；旧下载关系保留但隐藏，禁止新增不支持作品。开源作者卡片移除，可用静态作品归入CCBCM整理目录，详情保留原作者与MIT来源。
- GitHub 登录默认普通用户；creator_access 持久化申请、待审/通过/退回状态，仅管理员可审批。上传、内容写入、编辑、提交和公开上架均在服务端校验资格，已有账号也不会自动获批；允许所有者下架/删除旧内容。作品审核继续独立存在。
- 权限回归覆盖匿名、CSRF、伪造状态、自审/越权、重复审批、退回再申请、批准后上传与原有账号隔离。互动失败原因及两条接入路线见 INTERACTIVE-WALLPAPER-PLAN.md，尚未把WebGL装进插件。
- 最新修正：私有陆家嘴入口从创作中心移至所有者个人主页，仅自身登录可见，原城市路由权限保持。

### 关于页面文案更新
- 关于这里更新为当前收藏/下载、插件使用、创作者申请及作品审核规则，移除旧三维城市宣传和尚未开放的过时介绍。删除页脚来源许可入口，开源作品详情及许可文件保留。

### 真实创作者目录
- /api/creators 使用持久化账号、资料、头像与审批状态，只列管理员和已批准创作者；草稿/待审/下架不计入作品数量，CCBCM整理的5件内置作品单独计入。申请介绍、审核原因和内部ID不作为公开字段。
- 目录共用圆角卡片、固定标题/数量行高、两行简介和底部按钮。旧creator链接转用真实个人主页，避免头像和简介两套显示。
- 接口测试覆盖未审批不可见、审批后出现、公开字段与零公开作品计数；账号回归和构建通过。阶段2目录替换已推进，其余版本管理/多账号实机等仍未全部验收。

### 图片与视频统一发布（2026-09-13）
- 本次最新要求优先：开放 PNG/JPG 静态壁纸，与 MP4 共用上传、审核、公开上架、下载空间与“使用”。类型写入服务器元数据；旧视频保持兼容。插件按清单 MIME 缓存图片或视频，并保留完整性校验。
- 单文件上限 200 MiB（界面显示 200 MB）；删除账号 250 MB 与全站 8 GB 存量限制，不再显示账号已用进度。保留原每日20次新上传的频率保护，它不限制作品存量。
- 8 MiB R2 multipart 上传避免单请求/整文件内存限制；分块进度、原文件重试、服务器流式 SHA256 和格式检查，图片审核和详情共用预览框。
- 自动测试已覆盖完整200 MiB/25块、超限拒绝、PNG/JPEG上传审核上架、跨账号拒绝、分块缺失、格式伪装与hash错误；账号/网络/城市私有路由回归与构建通过。网页和安装包上线实机验证待接续记录。
- 流程仍有效：图片视频首期范围已实现，真实双账号、版本更新、插件网站状态回执等尚未全验收。

- 02b6e1e 已上线：上传区改为图片/视频共同入口，移除账号用量提示；线上 JPG 保存成功，图片原始尺寸1280，预览框16:9，未公开测试草稿。
- 用户随后要求“不用测试，先做该做的修改”：停止后续测试及正在进行的大文件浏览器上传。200 MiB 自动边界已通过，但不声称正式站200 MiB完整传输已验收。
- 继续完善失败恢复：分块会话可续传，超时会话重新建立并清理旧分块；过期上传允许移除，不再长期卡在上传中。插件“选择壁纸”文案及静态应用状态同步更新。
- 官网提供新安装包；本机运行安装包的操作被自动审批拒绝（未提供具体原因），未绕过、未更新本机插件。使用新图片能力需安装官网新版。
- 下一步按用户最新要求优先实现流程余项，暂不追加测试；整体续做约定仍生效。

### 发现页轮播与当天热度（2026-09-14）
- 保留首页原有网格、圆角、文案与卡片结构，仅在原背景区域加入静音视频轮播和上一张/下一张/暂停控件。每12秒切换；只载入当前视频，切换释放旧源，离屏/后台/详情弹窗暂停；减弱动态效果偏好默认暂停。
- 新增 D1 当天点击统计：北京时间自然日，实际打开作品详情才计数，自动轮播/曝光不计数；按每日网络地址和浏览器信息的加盐摘要去重，不存原始IP，属于基础防重复而非严格反刷。只统计公开且可用作品。
- 发现列表与视频候选分别按有效点击取前10，余项随机。随机顺序在本页浏览中稳定，刷新产生新顺序；同分按作品ID稳定排序，没有热度的作品随机补位。每5分钟读取最新统计，下一次渲染采用新排名，避免浏览途中卡片跳动。
- 按流程推进阶段3：未登录时点击“下载”保留15分钟内的选择，登录返回后保存到当前账号的私人下载空间，作品归属不变。
- 遵照用户“不用测试，先做修改”，本次不追加运行/浏览器测试，只构建发布；流程约定仍生效。

### 收藏合并到我的下载、爱心即时反馈（2026-09-14）
- 最新产品规则替代独立收藏空间：菜单只保留“我的下载”，旧 #saved 链接自动转入；收藏和下载在同一私人列表展示，但云端关系仍区分，不改作者归属。
- 新 /api/library 按当前会话合并两类记录，同一作品去重，收藏置顶；未下载收藏显示“下载”，已下载显示“使用”。取消收藏保留下载，移除下载保留收藏；下架作品也可取消收藏。
- 爱心从先读/写/再读改为即时乐观更新+单次写入；失败恢复并提示，单作品请求锁不阻塞其他作品，避免旧读取覆盖操作或退出后污染状态。
- 卡片标题、作者、类型统一行高，爱心放在标题右侧，操作行明确间距、窄屏允许换行。旧浏览器收藏的显式导入入口迁到我的下载，仍需本人确认归属，不自动导入。
- 按流程推进阶段1私人空间合并与异常反馈；继续遵照不追加测试的要求，只构建和推送，未标记全流程完成。

### GitHub 归属与跨电脑交接（2026-09-26）
- 已核对 CCBCM 主项目 `origin` 指向 `https://github.com/ccbcm/The-world.git`，当前生产分支为 `main`；本轮仅补充归属和部署说明，不改运行时代码、D1、R2、Resend 或 Pages 绑定。
- 新增 `CCBCM-DEPLOYMENT.md`，记录主仓库、Cloudflare Pages、D1/R2、Resend 和换电脑继续工作的边界；密钥仍只保留在线上环境。
- `piorunkulaga174` 账号的旧陆家嘴项目暂不删除、不迁移，避免影响该账号的其他项目。后续只有在确认没有域名、Pages、R2、D1 或其他依赖，并完成历史和素材备份后，才考虑归档或删除；该动作需要单独确认。
- 本阶段完成主仓库固定和交接文档，网站稳定性优先；后续开发继续以本文件、`AGENTS.md` 和 `CCBCM-DEPLOYMENT.md` 为准。
- 计算机使用核验确认：`ccbcm` 会话拥有仓库，Cloudflare Pages 连接 `ccbcm/The-world`、生产分支 `main` 且自动部署开启；桌面 Chrome 的旧会话无仓库设置权限，不需要把它当作主账号继续操作。

### 2026-09-26 首屏加载优化（进行中）
- 只读审计发现：`gallery/app.js` 原先等待 `loadPublished()` 与 `loadDiscovery()` 的 `Promise.all` 完成后才首次 `render()`；网络较慢时本地已有首页内容也不会显示。
- 本次小范围修复：先执行 `render()` 与待处理下载恢复，再异步加载公开作品和发现排名；公开作品返回后再刷新页面。未改数据关系、接口路径、素材和第三方依赖。
- 本地证据：`node --check gallery/app.js`、`npm run build`、`git diff --check` 通过；390px/本地浏览器首屏可见标题、英雄封面、分类和本地作品卡片。
- 尚未完成：线上提交与线上首屏性能数据核对；大图压缩、响应式图片/CDN 策略暂不改，需先做网络请求体积和视觉回归评估。
- 下一步：核对线上版本；随后再评估 `lines.png` 等大素材是否需要生成 WebP/AVIF 与 `srcset`，避免直接替换原始素材。

### 2026-09-26 静态资源缓存优化（进行中）
- 线上资源核对：`blue.jpg` 约 8 KB，首页本地首屏封面不是主要体积瓶颈；`lines.png` 约 1.5 MB，但当前首页使用 `lines.svg`，暂不替换原图。
- 已将 `/gallery/*` 的静态资源缓存改为 `max-age=86400, stale-while-revalidate=604800`，并把首页 CSS/模块脚本版本号更新为 `cache-2`，避免发布后继续使用旧代码。
- 保持 `/main.js`、`/character.js`、`/architecture.js`、`/city.html` 原有重新验证策略，避免扩大缓存范围。
- `npm run build`、`git diff --check` 通过；已推送提交 `38cdd80`、`b6ad9ce`。线上部署 HTTP 200 核对待下一次缓存头回读。
- 下一步：回读线上 `Cache-Control`，再决定是否做 WebP/AVIF 与 `srcset`；这会涉及资源生成和视觉回归，仍需单独评估。
